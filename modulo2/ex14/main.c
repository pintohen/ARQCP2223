#include <stdio.h>
#include "asm.h"

int main(){
    int area = getArea();
    printf("\nBase maior: %d\nBase menor: %d\nAltura: %d\n", length1, length2, height);
    printf("Area: %d\n", area);

    return 0;
}